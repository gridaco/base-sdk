/**
 * bridged manifest version. including this client-sdk and reflect's design manifest definitions major out-side-interface changes.
 */
export enum ManifestVersion {
    /**
     * unreleased manifest version based on nov 2020
     */
    v2020_0 = "v2020.0",
}