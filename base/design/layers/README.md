# Design Layer (different from layers/layer !)

> design layer contains the design's layer configuration. the layer under layers/ is a configuration of layer for interfacing with server side. (cautioin)